# chat-app-frontend
Real-time chat application built with React. Features JWT authentication, public group chat, private messaging, typing indicators, and read receipts using WebSocket (STOMP over SockJS).
